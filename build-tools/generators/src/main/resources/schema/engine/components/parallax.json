{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.components.Parallax",
    "type": "object",
    "extends": {
        "$ref": "component.json"
    },
    "properties": {
        "d": {
            "type": "number",
            "description": "Amount of pixels the x coordinate must be increased when the target entity moves along the x axis.",
            "default": 1.0
        }
    },
    "description": "Makes the visibility of the parent entity depend on a given condition. When the condition is evaluated to true, the entity will be shown in screen and touchable."
}